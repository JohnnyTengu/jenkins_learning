pipeline {
  agent any
  stages {
    stage('print hosts list') {
        steps {
          script {
            def browsers = System.getenv(HOSTS)
            echo "${browsers[i]}"

        }
    }
  }
}
}