pipeline {
    agent any 
    stages {
        stage('Wait...') { 
            steps {
              bash '''
              #!/bin/bash
              echo "Waiting"
              ```
            }
        }
        stage('Ping') { 
            steps {
              bash '''
              ping $WHAT_PING -c 4
              '''
              
                // 
            }
        }
    }
}
